services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: tennisbot
      POSTGRES_PASSWORD: tennisbot
      POSTGRES_DB: tennisbot
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tennisbot"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
